Based on the following content, answer the boolean 0/1 questions in the specified JSON format.
You must return a JSON object with a "res" array containing exactly {{ questions | length }} decision objects, one per question, in order.

Context:
{{ content }}

Questions:
{% for question in questions %}
- {{ question }}
{% endfor %}

For each question, produce one object with "res" as 0 or 1, "confidence" as your confidence between 0.0 and 1.0, and "reason" as one short sentence naming the decisive evidence — following the exact schema provided, with the objects in the same order as the questions.

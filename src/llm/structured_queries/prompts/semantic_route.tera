Decide which target should handle the item below.

Targets (name: what it handles):
{{ targets_display }}

Item:
{{ content }}

Each target's description is its contract — pick the single target whose contract covers this item; you cannot invent a target. Respond in the specified JSON format: "target" is the chosen target name, "confidence" is your confidence between 0.0 and 1.0, and "reason" is one short sentence naming why the item falls under that contract.

Decide whether the two items below refer to the same thing.

{% if context %}Judgment context (what "the same" means here): {{ context }}

{% endif %}Item A:
{{ left }}

Item B:
{{ right }}

Judge semantic identity, not textual equality: spelling variants, abbreviations, legal suffixes, reordering, and formatting differences do not make two references to one thing different. Two genuinely different things are not the same however similar they look.

Respond in the specified JSON format: "same" is true or false, "confidence" is your confidence between 0.0 and 1.0, and "reason" is one short sentence naming the decisive evidence.
